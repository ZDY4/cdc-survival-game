# 主线后续打磨项

当前范围只覆盖现有首段主线完成之后的后续连续性打磨，不回头重复已经打通的：

- `tutorial_survive`
- `zombie_hunter`
- `find_medicine`
- `factory_spare_power`

当前这轮已经完成：

- 医院段后的下一条主线任务已接通
- `factory` 已收口成可跑的第二圈层主线地图
- 出发前的补给准备压力已通过商店/据点补给调整收紧
- `factory` 完成后已有稳定的回据点后续钩子

本文件只保留还没做的尾项。

## 当前剩余问题

### 1. 陈医生还没有接入工厂段后的连续反馈

当前工厂段主要由老王承接，陈医生在医院段之后缺少新的阶段反馈。

这会导致：

- 两个核心 NPC 的主线交接感不够连续。
- 玩家完成工厂段后，从陈医生处拿不到新的世界反馈。

### 2. 据点完成工厂段后的状态变化还不够可感知

目前已经通过库存和文本把“出发准备”收紧了，但据点自身还缺少更明显的完成后变化。

这会导致：

- 玩家完成工厂段后的世界反馈仍偏弱。
- 据点不像一个随着主线推进而变化的节点。

### 3. 工厂目标区还可以再做得更醒目

当前 `factory` 已经可跑，但目标区主要靠路线和文本说明支撑，视觉辨识还可以更强。

## 剩余 TODO

### Must

- [ ] 给陈医生补工厂段进行中 / 完成后的连续反馈。
- [ ] 给据点补一处玩家可感知的工厂段完成后变化。

### Should

- [ ] 给 `factory` 控制间目标区补更强的视觉识别。
- [ ] 让工厂段完成后的下一站提示更具体，而不只是方向性提醒。

### Nice to have

- [ ] 给第二段回据点后的状态反馈补更多环境文案或 UI 文案。
