# 项目理解计划

为了深入理解 `cdc_survival_game` 工程，我将执行以下步骤：

1.  **项目结构概览**
    *   列出根目录下的文件和文件夹，识别主要模块。
    *   检查 `README.md`（如果存在）以获取项目文档。

2.  **核心配置分析**
    *   读取 `project.godot` 文件，了解项目设置、全局类（Global Classes）、自动加载（Autoloads）和输入映射（Input Map）。

3.  **代码库深度探索**
    *   分析 `core/` 目录（根据当前打开的文件推测存在），理解基础模块和核心逻辑。
    *   查找并分析主要场景文件（`.tscn`）和对应的脚本（`.gd`），理清游戏流程。
    *   识别关键的游戏机制（如生存要素、CDC 相关逻辑）。

4.  **总结与报告**
    *   汇总收集到的信息，生成一份项目架构和功能的总结报告。
